# ATS STM32H745 板卡配置
#
# SPDX-License-Identifier: Apache-2.0

if BOARD_ATS_STM32H745

# 在这里放“板卡级全局默认值”。
#
# 说明：
# - 这里适合放真正属于“板卡身份”的默认配置，通常对 M7/M4 都适用（除非在别处被覆盖）。
# - 与核相关的默认值一般放在：
#   ats_stm32h745_stm32h745xx_m7_defconfig / ats_stm32h745_stm32h745xx_m4_defconfig

endif # BOARD_ATS_STM32H745
